/**
 * Helper to format numbers nicely according to current locale
 * 
 * @author	toh
 */
String format(Integer this) :
	JAVA org.genesez.platform.common.statistic.FormatHelper.format(java.lang.Integer);