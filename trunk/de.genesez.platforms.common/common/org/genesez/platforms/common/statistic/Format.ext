/**
 * Helper to format numbers nicely according to current locale
 * 
 * @author	toh
 */
String format(Integer this) :
	JAVA org.genesez.platforms.common.statistic.FormatHelper.format(java.lang.Integer);