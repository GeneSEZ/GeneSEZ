<?xml version="1.0" ?>
<project name="de.genesez.metamodel.core" basedir=".." default="build">
	
	<condition property="build.basedir"
		value="${basedir}/..">
		<not>
			<isset property="build.basedir" />
		</not>
	</condition>
	
	<available file="${build.basedir}/de.genesez.build" type="dir" property="de.genesez.build.available" />
	<fail unless="de.genesez.build.available" message="Please make sure that ${build.basedir}/de.genesez.build is available" />
	
	<!-- Import project related properties -->
	<property file="build/build.properties" />
	
	<!-- Set the classpath -->
	<path id="de.genesez.metamodel.classpath">
		<pathelement location="${bin.dir}" />
		<pathelement location="${workflow.dir}" />
		<pathelement location="${model.dir}" />
		<pathelement location="${build.basedir}/org.eclipse.emf.ecore.adapter.uml/bin" />
		<pathelement location="${build.basedir}/org.eclipse.emf.ecore.adapter.uml/uml2ecore" />
		<path refid="Plug-in Dependencies.libraryclasspath" />
	</path>
	
	<import file="${build.basedir}/de.genesez.build/build/metamodel.xml"/>
	<import file="${build.basedir}/de.genesez.adapter.magicdraw/build/metamodel-export.xml"/>
	
	<target name="clean" depends="magicdraw.clean, de.genesez.metamodel.clean, clean.ecore" />
	<target name="uml2ecore" depends="magicdraw.metamodel.export, mwe2.workflow" />
	
	<target name="createjar"
		description="Create the plugin jar for de.genesez.metamodel"
		depends="build">
		<mkdir dir="${dist.dir}" />
		<jar destfile="${dist.dir}/${project.name}.jar" compress="true">
			<fileset dir="${bin.dir}" includes="**/*" />
		</jar>
		<jar destfile="${dist.dir}/${plugin.jar}"
			compress="true"
			manifest="META-INF/MANIFEST.MF">
			<fileset dir="${dist.dir}" includes="${project.name}.jar" />
			<fileset dir="${basedir}">
				<include name="${ecore.file}" />
				<include name="plugin.xml" />
			</fileset>
		</jar>
		<delete file="${dist.dir}/${project.name}.jar" />
	</target>
</project>
