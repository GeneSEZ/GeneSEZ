<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project name="org.eclipse.emf.ecore.adapter.uml" basedir=".." default="build">

	<condition property="build.basedir"
		value="${basedir}/..">
		<not>
			<isset property="build.basedir" />
		</not>
	</condition>

	<available file="${build.basedir}/org.genesez.build" type="dir" property="org.genesez.build.available" />
	<fail unless="org.genesez.build.available" message="Please make sure that ${build.basedir}/org.genesez.build is available" />

	<import file="${build.basedir}/org.genesez.build/build/imports.xml" />
	
	<!-- Import project related properties -->
	<property file="build.properties" />
	
	<!-- Set the classpath -->
	<path id="org.eclipse.emf.ecore.adapter.uml.classpath">
		<path refid="Plug-in Dependencies.libraryclasspath"/>
	</path>
	
	<target name="build" depends="compile"
		description="Build org.eclipse.emf.ecore.adapter.uml" />
	
	<target name="createjar"
		depends="build"
		description="Create the plugin jar for org.eclipse.emf.ecore.adapter.uml">
		<mkdir dir="${dist.dir}" />
		<jar destfile="${dist.dir}/${project.name}.jar" compress="true">
			<fileset dir="${bin.dir}" includes="**/*" />
		</jar>
		<jar destfile="${dist.dir}/${plugin.jar}"
			compress="true"
		 	manifest="META-INF/MANIFEST.MF">
			<fileset dir="${dist.dir}" includes="${project.name}.jar" />
			<fileset dir="${basedir}">
				<include name="${ecore.dir}/**" />
			</fileset>
		</jar>
		<delete file="${dist.dir}/${project.name}.jar" />
	</target>
	
	<target name="clean"
		description="Clean org.eclipse.emf.ecore.adapter.uml">
		<delete includeemptydirs="true" failonerror="false">
			<fileset dir="${bin.dir}" />
			<fileset dir="${dist.dir}" includes="*.jar" />
		</delete>
	</target>
	
	<!-- private target section -->
	
	<target name="compile">
		<mkdir dir="${bin.dir}" />
		<javac srcdir="${src.dir}" destdir="${bin.dir}"
			classpathref="org.eclipse.emf.ecore.adapter.uml.classpath"
			target="${target}"
			source="${source}"
			debug="on" />
	</target>
</project>