<?xml version="1.0"?>
<project name="de.genesez.build.environment" basedir=".." default="">

	<property file="${project.build.dir}/release.properties" />

	<!-- set the timestamps -->
	<tstamp>
		<format property="release.time" pattern="yyyyMMddhhmm"/>
	</tstamp>
	
	<property name="project.name" value="${ant.project.name}"/>
	<property name="plugin.version" value="${release}.${release.time}"/>
	<propertycopy name="plugin.version" from="plugin.version" override="true"/>
	<property name="plugin.name" value="${project.name}_${plugin.version}"/>
	<property name="plugin.jar" value="${plugin.name}.jar"/>
	
</project>
