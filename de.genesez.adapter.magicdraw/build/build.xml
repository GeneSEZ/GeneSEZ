<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project name="de.genesez.adapter.magicdraw" basedir=".." default="build">

	<condition property="build.basedir"
		value="${basedir}/..">
		<not>
			<isset property="build.basedir" />
		</not>
	</condition>

	<available file="${build.basedir}/de.genesez.build" type="dir" property="de.genesez.build.available" />
	<fail unless="de.genesez.build.available" message="Please make sure that ${build.basedir}/de.genesez.build is available" />

	<import file="${build.basedir}/de.genesez.build/build/imports.xml" />
	
	<!-- Import project related properties -->
	<property file="build.properties" />
	
	<!-- Set the classpath -->
	<path id="de.genesez.adapter.magicdraw.classpath">
		<path refid="Plug-in Dependencies.libraryclasspath"/>
	</path>
	
	<target name="build" description="Build de.genesez.adapter.magicdraw" />
	
	<target name="createjar"
		depends="build"
		description="Create the plugin jar for de.genesez.adapter.magicdraw">
		<mkdir dir="${dist.dir}" />
		<jar destfile="${dist.dir}/${plugin.jar}"
			compress="true"
		 	manifest="META-INF/MANIFEST.MF">
			<fileset dir="${build.dir}" includes="metamodel-export.xml" />
		</jar>
	</target>
	
	<target name="clean"
		description="Clean de.genesez.adapter.magicdraw">
		<delete includeemptydirs="true" failonerror="false">
			<fileset dir="${dist.dir}" includes="*.jar" />
		</delete>
	</target>
</project>