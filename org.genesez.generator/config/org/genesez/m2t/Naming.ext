
import gcore;

extension org::genesez::platform::java::util::Naming;

/// replace the naming of final attributes as constants by using variable
around org::genesez::platform::java::util::Naming::asAttribute(MAttribute this) :
	(static && final) || classifier.metaType == MInterface ? asConstant() : asVariable();
