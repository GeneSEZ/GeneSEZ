<?xml version="1.0" encoding="UTF-8"?>
<workflow>

	<!-- 
		executes a check script to validate the model against constraints
		
		following parameters are required:
		==================================
		
		$ea_model			name of the ea repository file
		$model_name			name of the model
		$profile_dir		directory name where profiles are located
		$profile_name		name of the profile to apply to the model
		$model_file			name of the xmi output file
	 -->

	<!-- initialize the support for UML2 models -->
	<bean class="org.openarchitectureware.uml2.Setup" standardUML2Setup="true" />
	
	<property name="ea_model_slot" value="de_genesez_adapter_ea_ea_model" />
	<property name="uml_model_slot" value="de_genesez_adapter_ea_uml_model" />
	<property name="resource_model_slot" value="de_genesez_adapter_ea_model_resource" />
	
	<component class="de.genesez.adapter.ea.RepositoryReader">
		<repositoryFile value="${ea_model}" />
		<modelName value="${model_name}" />
		<outputSlot value="${ea_model_slot}" />
	</component>

	<component class="de.genesez.adapter.ea.ProfileReader">
		<profileExtension value=".profile.uml"/>
		<profileDirectory value="${profile_dir}"/>
		<profile value="${profile_name}"/>
	</component>

	<component class="de.genesez.adapter.ea.Model2UML">
		<inputSlot value="${ea_model_slot}" />
		<outputSlot value="${uml_model_slot}" />
	</component>

	<component class="de.genesez.adapter.ea.XmiWriter">
		<inputSlot value="${uml_model_slot}" />
		<resourceSlot value="${resource_model_slot}" />
		<xmiFile value="${model_file}" />
	</component>

</workflow>