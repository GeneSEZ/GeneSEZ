svnant.jar = ${project.dir}/${lib.dir}/svnant.jar
svnClientAdapter.jar = ${project.dir}/${lib.dir}/svnClientAdapter.jar
svnjavahl.jar = ${project.dir}/${lib.dir}/svnjavahl.jar

svn.url = http://svn.genesez.de/genesez
svn.branches = ${svn.url}/branches
svn.tags = ${svn.url}/tags
svn.trunk = ${svn.url}/trunk
