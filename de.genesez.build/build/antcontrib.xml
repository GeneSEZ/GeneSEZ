<?xml version="1.0"?>
<project name="de.genesez.build.antcontrib" basedir=".." default="">

	<!--  all properties are in build.properties --> 
	<property file="${build.basedir}/build/antcontrib.properties" /> 

	<!-- path to the ant contrib libraries -->
	<path id="antcontrib.classpath">
		<pathelement location="${antcontrib.jar}" /> 
	</path>  
  
	<!-- load the ant contrib tasks -->
	<taskdef resource="net/sf/antcontrib/antcontrib.properties"
		classpathref="antcontrib.classpath"/>

</project>
